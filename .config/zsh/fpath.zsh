# .dotfile
# zsh/fpath.zsh

#add each topic folder to fpath so that they can add functions and completion scripts
# for topic_folder ($DOT/*) if [ -d $topic_folder ]; then
#   echo "adding $topic_folder to fpath"
#   fpath=($topic_folder $fpath)
# fi
