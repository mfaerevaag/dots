default  partial alphanumeric_keys modifier_keys
xkb_symbols "basic" {

    include "no(mac)"
    name[Group1]= "Norwegian (NO) Markus";

    key <TLDE> {	[     dollar,	section	]	};

    key <LSGT> {	[     grave,	asciitilde	]	};

    include "level3(ralt_switch)"
};
